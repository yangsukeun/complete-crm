import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Project detail | Complete CRM",
};

export default function TaskDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
