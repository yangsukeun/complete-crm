import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Projects | Complete CRM",
};

export default function TasksLayout({ children }: { children: React.ReactNode }) {
  return children;
}
