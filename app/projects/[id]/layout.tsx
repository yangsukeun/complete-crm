import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "CRM Project | Complete CRM",
};

export default function ProjectCrmLayout({ children }: { children: React.ReactNode }) {
  return children;
}
